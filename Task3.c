#include <stdio.h>
int main()
{
    int hight = 7, width = 5;

    printf("Perimeter of the rectangle = %d inches\n", 2 * (hight + width));
    printf("Area of the rectangle = %d square inch\n", hight * width);

    return 0;
}